'use strict';
/**
 * The top level api for dealing with a single document.
 */
function Document(model, seedProps) {
    this._model = model;
};
/**
 *
 */
Model.prototype.findById = function (id, callback) {

};

module.exports = Model;
